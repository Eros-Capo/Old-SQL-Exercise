SELECT NOW();
SELECT CURRENT_TIMESTAMP;

SELECT CURTIME();
SELECT CURDATE();


CREATE TABLE esempio(
  momento_arrivo DATETIME DEFAULT CURRENT_TIMESTAMP
);

SELECT TO_DAYS('1981-05-29'); # dall'anno zero

SELECT FROM_DAYS(100000);

SELECT DATEDIFF(NOW(),'2020-12-31');

SELECT TIMEDIFF(CURTIME(),'10:00:00');

SELECT TIMEDIFF('2019-03-04 05:00:00','2019-03-04 03:00:00');

SELECT DAYOFWEEK(NOW());

SELECT DAYOFMONTH('2019-03-04');

SELECT DAYOFYEAR(NOW());

SELECT DAY(NOW());

SELECT MONTH(NOW());

SELECT YEAR(NOW());

SELECT DATE(NOW());

SELECT TIME(NOW());

SELECT HOUR(NOW());
SELECT MINUTE(NOW());
SELECT SECOND(NOW());

SELECT TIME_TO_SEC('00:01:00');
SELECT SEC_TO_TIME(4560);

SELECT DATE_ADD(NOW(),INTERVAL 1.30 HOUR_MINUTE );
SELECT DATE_ADD(NOW(),INTERVAL -1 MONTH );

# QUARTER = 3 mesi
SELECT DATE_ADD(NOW(),INTERVAL 1 QUARTER );

SELECT DATE_ADD(NOW(),INTERVAL 3 HOUR );

SELECT TIMESTAMPDIFF(WEEK ,'2019-05-01 10:00:00',NOW());

SELECT TIMESTAMPADD(WEEK , 2 ,NOW());

SELECT TIMEDIFF('2019-03-04 05:00:00','2019-02-03 06:00:00');